// Package mq implements an in-memory message queue broker with HTTP transport,
// partition routing, consumer group management, and optional WAL persistence.
// All types shared with other packages live in internal/model to avoid circular imports.
package mq
